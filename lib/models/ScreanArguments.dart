class ScreenArguments {
  int totalPrice;
  String address;
  String documentId;
  String status;
  String userid;
  String image;

  ScreenArguments(this.totalPrice, this.address, this.documentId, this.status,
      this.userid, this.image);
}
